class Solution:
    def arraySign(self, nums: List[int]) -> int:
        l=0
        m=0
        for i in nums:
            if i < 0:
                l+=1
            elif i==0:
                m=0
                return 0
        if l%2==0:
            return +1
        else:
            return -1
